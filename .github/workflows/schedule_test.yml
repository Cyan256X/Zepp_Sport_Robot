name: schedule-test
on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: echo
        run: echo "schedule-test triggered at $(date -u)"
